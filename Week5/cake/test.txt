Я создал проект используя maven framework(Apache Maven — фреймворк для автоматизации сборки проектов на основе описания их структуры в файлах на языке POM, являющемся подмножеством XML)

Далее в файле pom.xml прописал для импортирования всех необходимых библиотек для работы с БД (hibernate, mysql-connector, maven-compiler-plugin)

Создал package Entity, где создал Java Class с POJO(задание 3-ей недели Часть 2)

Сконфигурировал файл для hibernate(там же происходит связь с БД(database name, username, password))

После этого создаю Main class в Java директории где связываюсь с БД cake_shop(произвожу действия добавить, обновить, удалить и вывожу лист employee)

В созданном ранее классе POJO(employee) в Entity связываюсь с БД


SessionFactory - Сеанс используется для получения физического соединения с базой данных. Объект Session является легким и предназначен для реализации каждый раз, когда необходимо взаимодействие с базой данных. Постоянные объекты сохраняются и извлекаются через объект Session(подробнее об этом можете узнать в tutorial hibernate)

createQuery("FROM tables.name") - Hibernate Query Language (HQL) - это объектно-ориентированный язык запросов, похожий на SQL, но вместо работы с таблицами и столбцами HQL работает с постоянными объектами и их свойствами. HQL-запросы преобразуются Hibernate в обычные SQL-запросы, которые в свою очередь выполняют действия с базой данных(подробнее об этом можете узнать в tutorial hibernate)



