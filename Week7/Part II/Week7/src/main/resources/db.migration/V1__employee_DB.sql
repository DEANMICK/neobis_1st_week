create table employee (
    id integer not null,
    address varchar(255),
    age integer,
    name varchar(255),
    phone varchar(255),
    position varchar(255),
    salary double precision,
    surname varchar(255),
    primary key (id)
    );